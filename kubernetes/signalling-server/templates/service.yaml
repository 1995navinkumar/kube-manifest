apiVersion: v1
kind: Service
metadata:
  name: signalling-server
  namespace: signalling-server

spec:
  ports:
    - name: web
      port: 80
      targetPort: web
    - name: ws
      port: 8080
      targetPort: ws

  selector:
    app: signalling-server
