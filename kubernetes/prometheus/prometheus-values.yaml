server:
  persistentVolume:
    enabled: false
prometheus-node-exporter:
  enabled: true
prometheus-pushgateway:
  enabled: false
alertmanager:
  enabled: false
kube-state-metrics:
  enabled: true
forceNamespace: prometheus
